[package]
name = "advanced-payment-system"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.5.0", features = ["alloc"] }
soroban-sdk-macros = "21.5.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
